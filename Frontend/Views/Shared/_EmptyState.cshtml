@model AMS.ViewModels.EmptyStateViewModel

<div class="empty-state">
    <div class="empty-icon">@Model.Icon</div>
    <h3 class="empty-title">@Model.Title</h3>
    <p class="empty-message">@Model.Message</p>
</div>